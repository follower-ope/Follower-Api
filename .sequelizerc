const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'deploy', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'deploy', 'app', 'models'),
  'migrations-path': resolve(__dirname, 'deploy', 'database', 'migrations'),
  'seeders-path': resolve(__dirname, 'deploy', 'database', 'seeds'),
}
